name: "CI"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  group: "ci-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  python:
    name: "Lint & Test (Python)"
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout"
        uses: actions/checkout@v4

      - name: "Set up Python"
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: "Install system dependencies"
        run: |
          sudo apt-get update
          sudo apt-get install -y libnss3-dev libatk-bridge2.0-dev libdrm2 libxcomposite1 libxdamage1 libxrandr2 libgbm1 libxss1 libasound2

      - name: "Install Python dependencies"
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install black ruff mypy pytest pytest-asyncio moto

      - name: "Install Playwright browsers"
        working-directory: backend
        run: |
          python -m playwright install chromium

      - name: "Black code formatting check"
        working-directory: backend
        run: black --check .

      - name: "Ruff linting check"
        working-directory: backend
        run: ruff check .

      - name: "MyPy type checking"
        working-directory: backend
        run: mypy app || true

      - name: "Run tests"
        working-directory: backend
        env:
          APP_ENV: test
          PYTHONPATH: .
        run: pytest tests/ -v || true

      - name: "Validate TASK.md Sync"
        if: github.event_name == 'pull_request'
        run: |
          # Check if code changes exist but TASK.md wasn't updated
          CODE_CHANGES=$(git diff --name-only origin/${{ github.base_ref }}..HEAD | grep -E "^(backend/app/|backend/infra/|backend/pyproject.toml)" | wc -l)
          TASK_CHANGES=$(git diff --name-only origin/${{ github.base_ref }}..HEAD | grep "TASK.md" | wc -l)
          
          echo "Code changes detected: $CODE_CHANGES"
          echo "TASK.md changes detected: $TASK_CHANGES"
          
          if [ $CODE_CHANGES -gt 0 ] && [ $TASK_CHANGES -eq 0 ]; then
            echo "❌ ERROR: Code changes detected but TASK.md not updated!"
            echo "Please update TASK.md to reflect completed work."
            echo "See PR template for guidance."
            exit 1
          fi
          
          if [ $TASK_CHANGES -gt 0 ]; then
            echo "✅ TASK.md updated - validating format..."
            # Check for proper completion markers
            if ! git diff origin/${{ github.base_ref }}..HEAD TASK.md | grep -E "(COMPLETED|\[x\])"; then
              echo "⚠️  WARNING: TASK.md changed but no completion markers found"
              echo "Make sure to check off completed items and update status"
            fi
          fi
          
          echo "✅ TASK.md validation passed"

  auto-update-tasks:
    name: "Auto-update TASK.md"
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    permissions:
      contents: write
      
    steps:
      - name: "Checkout"
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: "Set up Python"
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          
      - name: "Install dependencies"
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -e . || echo "Failed to install project, continuing..."
          
      - name: "Auto-update TASK.md"
        env:
          GITHUB_PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          if [ -f backend/scripts/auto_update_tasks.py ]; then
            python3 backend/scripts/auto_update_tasks.py || echo "Auto-update script failed, skipping..."
          else
            echo "Auto-update script not found, skipping..."
          fi
          
      - name: "Commit TASK.md updates"
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          if ! git diff --quiet TASK.md; then
            echo "📝 TASK.md was updated - committing changes"
            git add TASK.md
            git commit -m "auto: update TASK.md with completed criteria

Auto-generated by CI/CD based on detected completions
Updated after commit ${{ github.sha }}"
            git push
            echo "✅ TASK.md auto-update committed"
          else
            echo "ℹ️  No TASK.md updates needed"
          fi

  # terraform:
  #   name: "Terraform Validate & Plan" 
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: backend/infra
  #   # Disabled until AWS secrets are configured
  #   permissions:
  #     id-token: write
  #     contents: read
  #   steps:
  #     - name: "Checkout"
  #       uses: actions/checkout@v4
  #     - name: "Setup Terraform"
  #       uses: hashicorp/setup-terraform@v3
  #       with:
  #         terraform_version: "1.7.5"
  #     # Additional terraform steps commented out...